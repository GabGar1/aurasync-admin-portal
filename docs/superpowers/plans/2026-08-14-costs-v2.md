# Tela de Custos v2 (Motor de Custos completo) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Atualizar a tela de Custos para o Motor de Custos v2: componentes com os 6 tipos/9 categorias e campos novos, área de Subgrupos (CRUD + produtos + componentes em lote), Taxa de crédito editável, Fechamento mensal, badge de subgrupo nas associações, e camadas mensais no resumo financeiro do pedido.

**Architecture:** Backend primeiro (repo `aurasync-backend`, branch `feat/cost-subgroup-linking`): expor `subgroup_id` na resposta de produtos, adicionar `cost_component_name` às `monthly_allocations` e criar o endpoint `POST /cost-components/associate-product-batch` (1 produto → N componentes, idempotente). Depois frontend (repo `aurasync-admin-portal`, branch `develop`): tipos, formatters, schema zod, serviços, e UI — a página Custos vira um shell de `Tabs` com 4 áreas; novos componentes em `src/components/costs/`.

**Tech Stack:** Node/TS/Fastify/Knex + zod (backend, testes `node --test`); React/TS/shadcn/TanStack Query/RHF+zod (frontend, vitest).

## Global Constraints

- **Sem commits** — o usuário não autorizou commits; cada tarefa termina com verificação (testes + lint + build), não com commit.
- Backend: `npm test` e `npm run lint` no diretório `aurasync-backend`. Frontend: `npm test`, `npm run lint`, `npm run build` no diretório `aurasync-admin-portal`.
- **Nunca calcular custo/lucro/margem no front** — exibir somente valores da API (`formatCurrency`); decimais podem vir como string → `Number(...)` para exibição.
- Mutação via axios existente (CSRF automático); áreas de custo protegidas por `isAdmin` (a rota `/costs` já é `adminOnly` no sidebar).
- Labels PT-BR centralizados em `src/lib/formatters.ts`; endpoints somente em `src/services/api.ts`.
- Estados de loading/erro/vazio em toda lista/diálogo novo; TanStack Query com invalidação após sucesso.

---

## Task 1 (Backend): `subgroup_id` na resposta de produtos

**Files:**
- Modify: `aurasync-backend/src/schemas/product.schema.ts` (`ProductSchema.response` e `listResponse`)
- Test: `aurasync-backend/src/services/product-subgroup.service.integration.test.ts` (arquivo existe; adaptar ao setup real)

**Interfaces:**
- Produces: `Product` (resposta) ganha `subgroup_id: string | null` (opcional) — as rows já vêm do `select *`; o zod hoje descarta.

- [ ] **Step 1: Escrever o teste que falha**

Em `src/services/product-subgroup.service.integration.test.ts` (reusando o setup do arquivo: cria produto e subgrupo; se o arquivo não tiver produto, siga o padrão de `product.service.integration.test.ts`):

```ts
it("exposes subgroup_id on products after assignment", async () => {
  // setup real do arquivo: subgroup criado + produto criado
  await productSubgroupService.assignProductsToSubgroup(subgroup.id, [product.id]);
  const result: any = await productService.getProducts(1, 10, {});
  const found = result.products.find((p: any) => p.id === product.id);
  assert.ok(found);
  assert.strictEqual(found.subgroup_id, subgroup.id);
});
```

Run: `npm test`
Expected: FAIL — `subgroup_id` undefined (zod descarta).

- [ ] **Step 2: Adicionar o campo aos dois schemas de resposta**

Em `src/schemas/product.schema.ts`, dentro do objeto de `response` (após `category`):

```ts
subgroup_id: z.uuid().nullable().optional(),
```

E o mesmo no objeto interno de `listResponse` (após `category`).

- [ ] **Step 3: Rodar testes + lint**

Run: `npm test` → todos PASS. Run: `npm run lint` → sem erros.

---

## Task 2 (Backend): `cost_component_name` nas `monthly_allocations`

**Files:**
- Modify: `aurasync-backend/src/repositories/order.repository.ts` (`findById` e `findAll`)
- Modify: `aurasync-backend/src/schemas/order.schema.ts` (entrada `monthly_allocations`)
- Test: `aurasync-backend/src/services/order.service.integration.test.ts`

**Interfaces:**
- Produces: cada item de `monthly_allocations` ganha `cost_component_name?: string | null` (LEFT JOIN com `cost_components`).

- [ ] **Step 1: Escrever o teste que falha**

Em `order.service.integration.test.ts` (reusando o setup existente — order `mainOrderId`):

```ts
it("returns component name on monthly allocations", async () => {
  const [component] = await db("cost_components")
    .insert({ name: "Equipe", type: "MONTHLY_FIXED", category: "OPERATIONAL", value: 500 })
    .returning("*");
  await db("order_monthly_allocations").insert({
    order_id: mainOrderId,
    cost_component_id: component.id,
    amount: 25.0,
    period_start: "2026-08-01",
    period_end: "2026-08-31",
  });
  const order = await orderService.getOrderById(mainOrderId);
  assert.ok(order);
  const alloc = order.monthly_allocations.find((a: any) => a.cost_component_id === component.id);
  assert.ok(alloc);
  assert.strictEqual(alloc.cost_component_name, "Equipe");
});
```

(Ajuste `mainOrderId`/método ao setup real do arquivo; importe `db` como nos demais testes.)

Run: `npm test`
Expected: FAIL — `cost_component_name` undefined.

- [ ] **Step 2: Join com nome nas duas queries**

Em `findById`, troque:

```ts
const monthly_allocations = await db('order_monthly_allocations').where({ order_id: id });
```

por:

```ts
const monthly_allocations = await db('order_monthly_allocations')
  .leftJoin('cost_components', 'cost_components.id', 'order_monthly_allocations.cost_component_id')
  .where({ 'order_monthly_allocations.order_id': id })
  .select(
    'order_monthly_allocations.*',
    'cost_components.name as cost_component_name'
  );
```

E em `findAll` (dentro do map):

```ts
const monthly_allocations = await db('order_monthly_allocations')
  .leftJoin('cost_components', 'cost_components.id', 'order_monthly_allocations.cost_component_id')
  .where({ 'order_monthly_allocations.order_id': order.id })
  .select(
    'order_monthly_allocations.*',
    'cost_components.name as cost_component_name'
  );
```

- [ ] **Step 3: Schema**

Em `src/schemas/order.schema.ts`, no array `monthly_allocations`, adicione no objeto:

```ts
cost_component_name: z.string().nullable().optional(),
```

- [ ] **Step 4: Rodar testes + lint**

Run: `npm test` → todos PASS. Run: `npm run lint` → sem erros.

---

## Task 3 (Backend): Endpoint `POST /api/cost-components/associate-product-batch`

**Files:**
- Modify: `aurasync-backend/src/schemas/cost.schema.ts`
- Modify: `aurasync-backend/src/repositories/cost.repository.ts`
- Modify: `aurasync-backend/src/services/cost.service.ts`
- Modify: `aurasync-backend/src/routers/cost.router.ts`
- Test: `aurasync-backend/src/services/cost.service.integration.test.ts`

**Interfaces:**
- Produces: `POST /cost-components/associate-product-batch` body `{ product_id: uuid, cost_component_ids: uuid[], quantity? (default 1) }` → `201 { product_id, associations }`, idempotente (unique `[product_id, cost_component_id]`, `onConflict merge({ quantity })` — mesmo padrão do `associateSubgroupBatch`).

- [ ] **Step 1: Escrever os testes que falham**

Em `cost.service.integration.test.ts` (siga o setup real do arquivo para produto/componentes):

```ts
it("associates multiple components to one product in batch", async () => {
  const result = await costService.associateProductBatch({
    product_id: product.id,
    cost_component_ids: [componentA.id, componentB.id],
    quantity: 2,
  });
  assert.strictEqual(result.product_id, product.id);
  assert.strictEqual(result.associations.length, 2);
});

it("associateProductBatch is idempotent (updates quantity)", async () => {
  await costService.associateProductBatch({ product_id: product.id, cost_component_ids: [componentA.id], quantity: 2 });
  const again = await costService.associateProductBatch({ product_id: product.id, cost_component_ids: [componentA.id], quantity: 3 });
  assert.strictEqual(again.associations.length, 1);
  assert.strictEqual(again.associations[0]!.quantity, 3);
});

it("rejects unknown cost component in batch", async () => {
  await assert.rejects(
    costService.associateProductBatch({ product_id: product.id, cost_component_ids: ["00000000-0000-0000-0000-0000000000ff"] }),
    /not found/
  );
});
```

Run: `npm test`
Expected: FAIL — `associateProductBatch is not a function`.

- [ ] **Step 2: Schema**

Em `src/schemas/cost.schema.ts`, no objeto `CostSchema` (após `associateSubgroupBatch`):

```ts
associateProductBatch: z.object({
  product_id: z.uuid("Invalid product ID"),
  cost_component_ids: z.array(z.uuid()).min(1, "cost_component_ids is required"),
  quantity: z.number().int().positive().default(1),
}),
```

E no bloco de types exportados:

```ts
export type CostAssociateProductBatch = z.infer<typeof CostSchema.associateProductBatch>;
```

- [ ] **Step 3: Repository**

Em `src/repositories/cost.repository.ts`:

```ts
async associateProductBatch(productId: string, componentIds: string[], quantity: number) {
  const rows = componentIds.map(cid => ({ product_id: productId, cost_component_id: cid, quantity }));
  return db('product_cost_components')
    .insert(rows)
    .onConflict(['product_id', 'cost_component_id'])
    .merge({ quantity })
    .returning('*');
}
```

- [ ] **Step 4: Service**

Em `src/services/cost.service.ts`:

```ts
async associateProductBatch(data: CostAssociateProductBatch) {
  const validated = CostSchema.associateProductBatch.parse(data);
  const product = await productRepository.findById(validated.product_id);
  if (!product) throw new Error('Product not found');

  for (const componentId of validated.cost_component_ids) {
    const component = await costRepository.findComponentById(componentId);
    if (!component) throw new Error(`Cost component ${componentId} not found`);
  }

  const associations = await costRepository.associateProductBatch(
    validated.product_id,
    validated.cost_component_ids,
    validated.quantity
  );
  return { product_id: validated.product_id, associations };
}
```

(verifique os imports existentes do arquivo — `productRepository` já é importado.)

- [ ] **Step 5: Rota**

Em `src/routers/cost.router.ts`, após o `POST /associate-subgroup-batch` (mantendo o padrão `authenticate` + `requireRole` do arquivo):

```ts
fastify.post('/associate-product-batch', {
  onRequest: [fastify.authenticate],
  preHandler: [requireRole(['ADMIN', 'SUPER_ADMIN'])],
  schema: { body: CostSchema.associateProductBatch },
}, async (request, reply) => {
  try {
    const result = await costService.associateProductBatch(request.body);
    return reply.code(201).send(result);
  } catch (error: any) {
    return reply.code(400).send({ error: error.message });
  }
});
```

- [ ] **Step 6: Rodar testes + lint**

Run: `npm test` → todos PASS. Run: `npm run lint` → sem erros.

---

## Task 4 (Frontend): Tipos (`src/types/index.ts`)

**Files:**
- Modify: `aurasync-admin-portal/src/types/index.ts`

**Interfaces:**
- Produces (consumidos nas Tasks 7–14): `ProductSubgroup`, `ProductSubgroupPayload`, `SubgroupAssociation`, `CreditFeeTier`, `CreditFeeTierUpdate`, `CostClosingInput`, `CostClosingComponent`, `CostClosingResponse`, `MonthlyAllocation`; `Product.subgroup_id?`; `Order.monthly_cost_total?`, `Order.total_cost_with_monthly?`, `Order.monthly_allocations?`, `Order.is_fair?`; unions e campos novos de `CostComponent`.

- [ ] **Step 1: Editar os tipos existentes**

```ts
export type CostComponentType = 'FIXED' | 'PERCENT' | 'PER_ORDER' | 'PACKAGING' | 'MONTHLY_FIXED' | 'MONTHLY_PERCENT';
export type CostComponentCategory = 'PACKAGING' | 'TAX' | 'FEE' | 'SHIPPING' | 'OPERATIONAL' | 'MARKETING' | 'OTHER' | 'ACQUISITION' | 'CREDIT_FEE';
export type AllocationBasis = 'PER_ORDER' | 'PER_PRODUCT';
```

Em `CostComponent` (após `is_active`):

```ts
max_products_per_package: number | null;
consolidates: boolean;
allocation_basis: AllocationBasis | null;
period_start: string | null;
period_end: string | null;
applies_to_fair_only: boolean;
```

Em `CostComponentPayload` (opcionais):

```ts
max_products_per_package?: number | null;
consolidates?: boolean;
allocation_basis?: AllocationBasis | null;
period_start?: string | null;
period_end?: string | null;
applies_to_fair_only?: boolean;
```

Em `Product`:

```ts
subgroup_id?: string | null;
```

Em `Order` (após `margin_percent?`):

```ts
is_fair?: boolean | null;
monthly_cost_total?: number;
total_cost_with_monthly?: number;
monthly_allocations?: MonthlyAllocation[];
```

- [ ] **Step 2: Adicionar os tipos novos**

```ts
export interface ProductSubgroup {
  id: string;
  name: string;
  description: string | null;
  is_active: boolean;
  created_at?: string;
  updated_at?: string;
}

export interface ProductSubgroupPayload {
  name: string;
  description?: string;
  is_active?: boolean;
}

export interface SubgroupAssociation {
  id: string;
  subgroup_id: string;
  cost_component_id: string;
  quantity: number;
  component?: CostComponent | null;
}

export interface CreditFeeTier {
  id: string;
  installments: number;
  percent: number;
  fixed_fee: number;
  is_active: boolean;
}

export interface CreditFeeTierUpdate {
  percent?: number;
  fixed_fee?: number;
  is_active?: boolean;
}

export interface CostClosingInput {
  month?: string;
  start_date?: string;
  end_date?: string;
}

export interface CostClosingComponent {
  id: string;
  name: string;
  type: string;
  allocation_basis: string | null;
}

export interface CostClosingResponse {
  period: { start: string; end: string };
  components: CostClosingComponent[];
  orders: number;
  products: number;
  allocations: number;
}

export interface MonthlyAllocation {
  id: string;
  order_id: string;
  cost_component_id: string;
  amount: number;
  period_start: string;
  period_end: string;
  cost_component_name?: string | null;
}
```

- [ ] **Step 3: Verificação**

Run: `npm run lint`, `npx tsc --noEmit -p tsconfig.app.json` (ou `npm run build`) — sem erros.

---

## Task 5 (Frontend): Formatters (`src/lib/formatters.ts` + testes)

**Files:**
- Modify: `src/lib/formatters.ts`
- Test: `src/lib/formatters.test.ts`

- [ ] **Step 1: Escrever os testes que falham**

Em `formatters.test.ts` (import `allocationBasisLabel`):

```ts
it('traduz base de rateio mensal', () => {
  expect(allocationBasisLabel('PER_ORDER')).toBe('Por pedido');
  expect(allocationBasisLabel('PER_PRODUCT')).toBe('Por produto vendido');
  expect(allocationBasisLabel('OUTRO')).toBe('OUTRO');
});

it('traduz categorias novas', () => {
  expect(categoryLabel('ACQUISITION')).toBe('Custo de aquisição');
  expect(categoryLabel('CREDIT_FEE')).toBe('Taxa de crédito');
});
```

Run: `npm test` → FAIL.

- [ ] **Step 2: Implementar**

Em `categoryLabels`, adicione `ACQUISITION: 'Custo de aquisição'`, `CREDIT_FEE: 'Taxa de crédito'`. Adicione:

```ts
const allocationBasisLabels: Record<string, string> = {
  PER_ORDER: 'Por pedido',
  PER_PRODUCT: 'Por produto vendido',
};

export function allocationBasisLabel(basis: string | null | undefined): string {
  if (!basis) return '-';
  return allocationBasisLabels[basis] ?? basis;
}
```

- [ ] **Step 3: Verificação**

Run: `npm test` → PASS. Run: `npm run lint` → sem erros.

---

## Task 6 (Frontend): Schemas (`src/lib/schemas.ts` + testes)

**Files:**
- Modify: `src/lib/schemas.ts`
- Test: `src/lib/schemas.test.ts`

**Interfaces:**
- Produces: `costComponentSchema` novo (6 tipos, 9 categorias, superRefine condicional, período regex), `productSubgroupSchema`, `CostComponentFormValues`, `ProductSubgroupFormValues`.

- [ ] **Step 1: Escrever os testes que falham**

Em `schemas.test.ts` (imports: `costComponentSchema, productSubgroupSchema`):

```ts
it('aceita componente PACKAGING com capacidade', () => {
  const result = costComponentSchema.safeParse({
    name: 'Caixa', type: 'PACKAGING', category: 'PACKAGING',
    value: 5, max_products_per_package: 6, consolidates: true,
  });
  expect(result.success).toBe(true);
});

it('exige capacidade para PACKAGING', () => {
  const result = costComponentSchema.safeParse({ name: 'Caixa', type: 'PACKAGING', value: 5 });
  expect(result.success).toBe(false);
});

it('exige base de rateio para MONTHLY_FIXED', () => {
  const without = costComponentSchema.safeParse({ name: 'Equipe', type: 'MONTHLY_FIXED', value: 500 });
  expect(without.success).toBe(false);
  const withBasis = costComponentSchema.safeParse({ name: 'Equipe', type: 'MONTHLY_FIXED', value: 500, allocation_basis: 'PER_ORDER' });
  expect(withBasis.success).toBe(true);
});

it('valida período como AAAA-MM-DD', () => {
  const bad = costComponentSchema.safeParse({ name: 'Tráfego', type: 'FIXED', value: 10, period_start: '01-08-2026' });
  expect(bad.success).toBe(false);
  const good = costComponentSchema.safeParse({ name: 'Tráfego', type: 'FIXED', value: 10, period_start: '2026-08-01', period_end: '2026-08-31' });
  expect(good.success).toBe(true);
});

it('rejeita tipo antigo MONTHLY', () => {
  const result = costComponentSchema.safeParse({ name: 'Antigo', type: 'MONTHLY', value: 1 });
  expect(result.success).toBe(false);
});

it('valida subgrupo', () => {
  expect(productSubgroupSchema.safeParse({ name: 'Anéis' }).success).toBe(true);
  expect(productSubgroupSchema.safeParse({ name: '' }).success).toBe(false);
});
```

Run: `npm test` → FAIL.

- [ ] **Step 2: Reescrever `costComponentSchema` e adicionar `productSubgroupSchema`**

```ts
export const costComponentSchema = z.object({
  name: z.string().min(1, 'Nome é obrigatório').max(100, 'Máximo de 100 caracteres'),
  description: z.string().max(500, 'Máximo de 500 caracteres').optional().or(z.literal('')),
  type: z.enum(['FIXED', 'PERCENT', 'PER_ORDER', 'PACKAGING', 'MONTHLY_FIXED', 'MONTHLY_PERCENT']),
  category: z.enum(['PACKAGING', 'TAX', 'FEE', 'SHIPPING', 'OPERATIONAL', 'MARKETING', 'OTHER', 'ACQUISITION', 'CREDIT_FEE']).default('OTHER'),
  value: z.coerce.number().min(0, 'Valor não pode ser negativo'),
  calculation_base: z.enum(['PRICE', 'COST']).optional(),
  is_active: z.boolean().default(true),
  max_products_per_package: z.preprocess(
    (v) => (v === '' || v === null ? undefined : v),
    z.coerce.number({ invalid_type_error: 'Capacidade inválida' }).int('Capacidade inválida').positive('Capacidade deve ser positiva')
  ).optional(),
  consolidates: z.boolean().default(false),
  allocation_basis: z.enum(['PER_ORDER', 'PER_PRODUCT']).optional(),
  period_start: z.string().regex(/^\d{4}-\d{2}-\d{2}$/, 'Data inválida (AAAA-MM-DD)').optional().or(z.literal('')),
  period_end: z.string().regex(/^\d{4}-\d{2}-\d{2}$/, 'Data inválida (AAAA-MM-DD)').optional().or(z.literal('')),
  applies_to_fair_only: z.boolean().default(false),
}).superRefine((data, ctx) => {
  if (data.type === 'PERCENT' && data.calculation_base === undefined) {
    ctx.addIssue({ code: 'custom', path: ['calculation_base'], message: 'Base de cálculo é obrigatória para componentes percentuais' });
  }
  if (data.type === 'MONTHLY_FIXED' && data.allocation_basis === undefined) {
    ctx.addIssue({ code: 'custom', path: ['allocation_basis'], message: 'Base de rateio é obrigatória para componentes mensais fixos' });
  }
  if (data.type === 'PACKAGING' && data.max_products_per_package === undefined) {
    ctx.addIssue({ code: 'custom', path: ['max_products_per_package'], message: 'Capacidade por caixa é obrigatória para embalagens' });
  }
});

export type CostComponentFormValues = z.infer<typeof costComponentSchema>;

export const productSubgroupSchema = z.object({
  name: z.string().min(1, 'Nome é obrigatório').max(100, 'Máximo de 100 caracteres'),
  description: z.string().max(500, 'Máximo de 500 caracteres').optional().or(z.literal('')),
  is_active: z.boolean().default(true),
});

export type ProductSubgroupFormValues = z.infer<typeof productSubgroupSchema>;
```

(Substitua o `costComponentSchema` antigo e seu type exportado.)

- [ ] **Step 3: Verificação**

Run: `npm test` → PASS (os testes antigos de `costComponentSchema` devem continuar passando). Run: `npm run lint`.

---

## Task 7 (Frontend): Serviços (`src/services/api.ts`)

**Files:**
- Modify: `src/services/api.ts`

- [ ] **Step 1: Adicionar os três novos grupos de serviço e os métodos no `costComponentsApi`**

No import de types do arquivo, adicione `ProductSubgroup, ProductSubgroupPayload, SubgroupAssociation, CreditFeeTier, CreditFeeTierUpdate, CostClosingInput, CostClosingResponse`.

Dentro de `costComponentsApi` (após `associate`):

```ts
associateSubgroup: async (payload: { subgroup_id: string; cost_component_id: string; quantity: number }): Promise<SubgroupAssociation> => {
  const response = await api.post<SubgroupAssociation>('/cost-components/associate-subgroup', payload);
  return response.data;
},

associateSubgroupBatch: async (payload: { subgroup_id: string; cost_component_ids: string[]; quantity: number }): Promise<{ subgroup_id: string; associations: SubgroupAssociation[] }> => {
  const response = await api.post<{ subgroup_id: string; associations: SubgroupAssociation[] }>('/cost-components/associate-subgroup-batch', payload);
  return response.data;
},

removeSubgroupAssociations: async (payload: { subgroup_id: string; cost_component_ids: string[] }): Promise<void> => {
  await api.delete('/cost-components/associate-subgroup-batch', { data: payload });
},

getBySubgroup: async (subgroupId: string): Promise<{ associations: SubgroupAssociation[] }> => {
  const response = await api.get<{ associations: SubgroupAssociation[] }>(`/cost-components/subgroup/${subgroupId}`);
  return response.data;
},

associateBatch: async (payload: { cost_component_id: string; product_ids?: string[]; subgroup_ids?: string[]; quantity: number }): Promise<{ product: { product_id: string }[]; subgroup: { subgroup_id: string }[] }> => {
  const response = await api.post<{ product: { product_id: string }[]; subgroup: { subgroup_id: string }[] }>('/cost-components/associate-batch', payload);
  return response.data;
},

associateProductBatch: async (payload: { product_id: string; cost_component_ids: string[]; quantity: number }): Promise<{ product_id: string; associations: CostAssociation[] }> => {
  const response = await api.post<{ product_id: string; associations: CostAssociation[] }>('/cost-components/associate-product-batch', payload);
  return response.data;
},
```

Após `costComponentsApi`:

```ts
export const productSubgroupsApi = {
  list: async (params?: { is_active?: boolean; search?: string }): Promise<ProductSubgroup[]> => {
    const response = await api.get<ProductSubgroup[]>('/product-subgroups', { params });
    return response.data;
  },
  create: async (payload: ProductSubgroupPayload): Promise<ProductSubgroup> => {
    const response = await api.post<ProductSubgroup>('/product-subgroups', payload);
    return response.data;
  },
  update: async (id: string, payload: Partial<ProductSubgroupPayload>): Promise<ProductSubgroup> => {
    const response = await api.put<ProductSubgroup>(`/product-subgroups/${id}`, payload);
    return response.data;
  },
  remove: async (id: string): Promise<void> => {
    await api.delete(`/product-subgroups/${id}`);
  },
  assignProducts: async (id: string, product_ids: string[]): Promise<{ assigned: number }> => {
    const response = await api.post<{ assigned: number }>(`/product-subgroups/${id}/products`, { product_ids });
    return response.data;
  },
  listProducts: async (id: string, params?: { page?: number; limit?: number; search?: string }): Promise<GetProductsResponse> => {
    const response = await api.get<GetProductsResponse>(`/product-subgroups/${id}/products`, { params });
    return response.data;
  },
  unassignProduct: async (id: string, productId: string): Promise<void> => {
    await api.delete(`/product-subgroups/${id}/products/${productId}`);
  },
};

export const creditFeeTiersApi = {
  list: async (): Promise<CreditFeeTier[]> => {
    const response = await api.get<CreditFeeTier[]>('/credit-fee-tiers');
    return response.data;
  },
  update: async (id: string, payload: CreditFeeTierUpdate): Promise<CreditFeeTier> => {
    const response = await api.put<CreditFeeTier>(`/credit-fee-tiers/${id}`, payload);
    return response.data;
  },
};

export const costClosingApi = {
  close: async (payload: CostClosingInput): Promise<CostClosingResponse> => {
    const response = await api.post<CostClosingResponse>('/cost-closing', payload);
    return response.data;
  },
};
```

- [ ] **Step 2: Verificação**

Run: `npm run lint` e `npm run build` — sem erros.

---

## Task 8 (Frontend): `CostComponentFormDialog` com campos condicionais

**Files:**
- Modify: `src/components/costs/CostComponentFormDialog.tsx`

- [ ] **Step 1: Atualizar defaults, reset e payload**

`defaultValues` e os dois branches do `form.reset`: incluir `max_products_per_package: undefined, consolidates: false, allocation_basis: undefined, period_start: '', period_end: '', applies_to_fair_only: false` (para edição, mapear de `component.*` com `?? ''` para períodos e `?? undefined` para capacidade/base de rateio).

Em `handleSubmit`, o payload passa a incluir:

```ts
const payload: CostComponentPayload = {
  name: values.name.trim(),
  description: values.description?.trim() ? values.description.trim() : undefined,
  type: values.type,
  category: values.category,
  value: values.value,
  is_active: values.is_active,
  consolidates: values.consolidates,
  applies_to_fair_only: values.applies_to_fair_only,
  period_start: values.period_start ? values.period_start : undefined,
  period_end: values.period_end ? values.period_end : undefined,
};
if (values.type === 'PERCENT' && values.calculation_base) {
  payload.calculation_base = values.calculation_base;
}
if (values.type === 'PACKAGING' && values.max_products_per_package !== undefined) {
  payload.max_products_per_package = values.max_products_per_package;
}
if (values.type === 'MONTHLY_FIXED' && values.allocation_basis) {
  payload.allocation_basis = values.allocation_basis;
}
```

- [ ] **Step 2: Tipo e categoria com 6/9 opções**

Select de tipo: `(['FIXED', 'PERCENT', 'PER_ORDER', 'PACKAGING', 'MONTHLY_FIXED', 'MONTHLY_PERCENT'] as const)`.
Select de categoria: `(['PACKAGING', 'TAX', 'FEE', 'SHIPPING', 'OPERATIONAL', 'MARKETING', 'OTHER', 'ACQUISITION', 'CREDIT_FEE'] as const)`.
Label do campo Valor: `selectedType === 'PERCENT' || selectedType === 'MONTHLY_PERCENT' ? 'Valor (%)' : 'Valor (R$)'`.

- [ ] **Step 3: Campos condicionais por tipo**

Após o grid de valor/base de cálculo:

```tsx
{selectedType === 'PACKAGING' ? (
  <div className="grid grid-cols-2 gap-4">
    <FormField
      control={form.control}
      name="max_products_per_package"
      render={({ field }) => (
        <FormItem>
          <FormLabel>Máx. produtos por caixa</FormLabel>
          <FormControl>
            <Input type="number" min="1" placeholder="Ex: 6" {...field} />
          </FormControl>
          <FormMessage />
        </FormItem>
      )}
    />
    <FormField
      control={form.control}
      name="consolidates"
      render={({ field }) => (
        <FormItem className="flex items-center justify-between rounded-lg border p-3">
          <div className="space-y-0.5">
            <FormLabel>Caixa consolidadora</FormLabel>
            <p className="text-xs text-muted-foreground">Absorve os itens dos demais subgrupos</p>
          </div>
          <FormControl>
            <Switch checked={field.value} onCheckedChange={field.onChange} />
          </FormControl>
        </FormItem>
      )}
    />
  </div>
) : null}

{selectedType === 'MONTHLY_FIXED' ? (
  <FormField
    control={form.control}
    name="allocation_basis"
    render={({ field }) => (
      <FormItem>
        <FormLabel>Base de rateio</FormLabel>
        <Select value={field.value} onValueChange={field.onChange}>
          <FormControl><SelectTrigger><SelectValue placeholder="Selecione..." /></SelectTrigger></FormControl>
          <SelectContent>
            <SelectItem value="PER_ORDER">Por pedido</SelectItem>
            <SelectItem value="PER_PRODUCT">Por produto vendido</SelectItem>
          </SelectContent>
        </Select>
        <FormMessage />
      </FormItem>
    )}
  />
) : null}
```

E para todos os tipos, após o switch Ativo:

```tsx
<FormField
  control={form.control}
  name="applies_to_fair_only"
  render={({ field }) => (
    <FormItem className="flex items-center justify-between rounded-lg border p-3">
      <div className="space-y-0.5">
        <FormLabel>Somente vendas de Feira</FormLabel>
        <p className="text-xs text-muted-foreground">Incide apenas quando o pedido é marcado como Feira</p>
      </div>
      <FormControl>
        <Switch checked={field.value} onCheckedChange={field.onChange} />
      </FormControl>
    </FormItem>
  )}
/>
<div className="grid grid-cols-2 gap-4">
  <FormField
    control={form.control}
    name="period_start"
    render={({ field }) => (
      <FormItem>
        <FormLabel>Início do período</FormLabel>
        <FormControl>
          <Input type="date" {...field} />
        </FormControl>
        <FormMessage />
      </FormItem>
    )}
  />
  <FormField
    control={form.control}
    name="period_end"
    render={({ field }) => (
      <FormItem>
        <FormLabel>Fim do período</FormLabel>
        <FormControl>
          <Input type="date" {...field} />
        </FormControl>
        <FormMessage />
      </FormItem>
    )}
  />
</div>
```

(Imports: `allocationBasisLabel` não é necessário aqui pois os labels estão fixos nos SelectItem.)

- [ ] **Step 4: Verificação**

Run: `npm test`, `npm run lint`, `npm run build` — sem erros.

---

## Task 9 (Frontend): `Costs.tsx` → shell de Tabs + `ComponentsTab`

**Files:**
- Create: `src/components/costs/ComponentsTab.tsx`
- Modify: `src/pages/Costs.tsx`

- [ ] **Step 1: Extrair o conteúdo atual para `ComponentsTab.tsx`**

Mova TODO o conteúdo atual de `Costs.tsx` (estados, queries, mutations, tabela, dialogs) para `src/components/costs/ComponentsTab.tsx` como `export default function ComponentsTab()`, com duas melhorias na tabela:
- Célula Valor: `component.type === 'PERCENT' || component.type === 'MONTHLY_PERCENT' ? `${component.value}%` : formatCurrency(component.value)`.
- Célula Tipo: texto principal `typeLabel(component.type)` + secundário (text-xs) quando aplicável:
  - `PACKAGING`: `Máx. ${component.max_products_per_package}/caixa` + `· Consolidada` se `consolidates`.
  - `MONTHLY_FIXED`: `allocationBasisLabel(component.allocation_basis)`.
  - `applies_to_fair_only`: badge "Feira".

(Importe `allocationBasisLabel` de `@/lib/formatters`.)

- [ ] **Step 2: `Costs.tsx` vira shell de Tabs**

```tsx
import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs';
import ComponentsTab from '@/components/costs/ComponentsTab';
import SubgroupsTab from '@/components/costs/SubgroupsTab';
import CreditFeeTab from '@/components/costs/CreditFeeTab';
import CostClosingTab from '@/components/costs/CostClosingTab';

export default function Costs() {
  return (
    <div className="flex flex-col p-6 space-y-6 motion-safe:animate-fade-in-up">
      <div className="shrink-0">
        <h1 className="text-2xl font-semibold">Custos</h1>
        <p className="text-sm text-muted-foreground">Componentes, subgrupos e apuração mensal de custos</p>
      </div>
      <Tabs defaultValue="components" className="w-full">
        <TabsList>
          <TabsTrigger value="components">Componentes</TabsTrigger>
          <TabsTrigger value="subgroups">Subgrupos</TabsTrigger>
          <TabsTrigger value="credit-fee">Taxa de Crédito</TabsTrigger>
          <TabsTrigger value="closing">Fechamento Mensal</TabsTrigger>
        </TabsList>
        <TabsContent value="components"><ComponentsTab /></TabsContent>
        <TabsContent value="subgroups"><SubgroupsTab /></TabsContent>
        <TabsContent value="credit-fee"><CreditFeeTab /></TabsContent>
        <TabsContent value="closing"><CostClosingTab /></TabsContent>
      </Tabs>
    </div>
  );
}
```

(Os imports dos outros tabs falharão até as Tasks 10–12 — para o lint/build desta task, crie os três arquivos como stubs mínimos: `export default function XTab() { return null; }` e os complete nas tasks seguintes.)

- [ ] **Step 3: Verificação**

Run: `npm test`, `npm run lint`, `npm run build` — sem erros.

---

## Task 10 (Frontend): Área Subgrupos (SubgroupsTab + 3 diálogos)

**Files:**
- Create: `src/components/costs/SubgroupsTab.tsx`
- Create: `src/components/costs/SubgroupFormDialog.tsx`
- Create: `src/components/costs/SubgroupProductsDialog.tsx`
- Create: `src/components/costs/SubgroupComponentsDialog.tsx`

**Interfaces:**
- Consumes: `productSubgroupsApi`, `costComponentsApi` (batch de subgrupo), `productsApi`, `productSubgroupSchema` (Task 6), `typeLabel`/`formatCurrency` (Task 5).
- Produces: tab completa (lista + CRUD + atribuição de produtos em lote + componentes em lote).

- [ ] **Step 1: `SubgroupFormDialog.tsx`** — mesmo padrão do CreateCustomerDialog (Dialog + RHF + zod `productSubgroupSchema`): campos Nome*, Descrição, switch Ativo; props `{ open, onOpenChange, subgroup: ProductSubgroup | null, isPending, onSubmit(payload: ProductSubgroupPayload) }`; reset no open (e no fechamento via `useEffect` para não manter valores antigos).

- [ ] **Step 2: `SubgroupProductsDialog.tsx`** — props `{ open, onOpenChange, subgroup: ProductSubgroup | null }`:
  - Lista dos produtos do subgrupo: query `['subgroup-products', subgroup?.id, page, search]` → `productSubgroupsApi.listProducts` com paginação simples (Anterior/Próximo) e busca; cada linha com botão desvincular (`unassignProduct` → toast + invalidação).
  - Seção "Atribuir produtos": busca de produtos (`productsApi.getAll({ search, limit: 10 })` — query habilitada quando a busca/seção está ativa), lista com `Checkbox` (import de `@/components/ui/checkbox`), estado `Set<string>` de selecionados; botão "Atribuir (N)" chama `assignProducts(subgroup.id, [...selected])` → toast `{assigned} produto(s) atribuído(s)` + limpar seleção + invalidar `['subgroup-products', ...]` e `['products']`.

- [ ] **Step 3: `SubgroupComponentsDialog.tsx`** — props `{ open, onOpenChange, subgroup: ProductSubgroup | null }`:
  - Lista associações: query `['subgroup-components', subgroup?.id]` → `costComponentsApi.getBySubgroup`; renderiza `associations` em tabela (Componente (component?.name), Tipo (`typeLabel`), Qtd) com `Checkbox` por linha para remoção em lote.
  - "Adicionar componentes": lista multi-select (Checkbox) dos componentes ativos não associados (`costComponentsApi.list({ is_active: true })` filtrado) + input Qtd único → `associateSubgroupBatch({ subgroup_id, cost_component_ids, quantity })` → toast + invalidação.
  - "Remover selecionados (N)": `removeSubgroupAssociations({ subgroup_id, cost_component_ids })` → toast + invalidação.

- [ ] **Step 4: `SubgroupsTab.tsx`**:
  - Query `['product-subgroups', debouncedSearch, statusFilter]` → `productSubgroupsApi.list`; busca (useDebounce) + Select status (Todos/Ativos/Inativos).
  - Tabela: Nome (+descrição), Status (badge), Ações (dropdown: Editar, Produtos, Componentes, Excluir — só `admin`).
  - Botão "Novo Subgrupo"; dialog de exclusão (AlertDialog, `remove` → 204).
  - Estado: `formOpen/editing`, `productsFor`, `componentsFor`, `deleting` (padrão de Costs.tsx).

- [ ] **Step 5: Verificação**

Run: `npm test`, `npm run lint`, `npm run build` — sem erros.

---

## Task 11 (Frontend): Área Taxa de Crédito (`CreditFeeTab`)

**Files:**
- Create: `src/components/costs/CreditFeeTab.tsx`

- [ ] **Step 1: Implementar a tab**

```tsx
const { data: tiers, isLoading, isError, refetch } = useQuery({
  queryKey: ['credit-fee-tiers'],
  queryFn: creditFeeTiersApi.list,
});
```

- Aviso permanente (Alert): "Alterações valem para novas vendas. Pedidos anteriores permanecem com os valores congelados da época."
- Tabela: Parcelas (`1x (à vista)` para installments===1, senão `${n}x`), Percentual (`Number(tier.percent).toFixed(2)%`), Taxa fixa (`formatCurrency(Number(tier.fixed_fee))`), Status (Ativo/Inativo badge), Ações (Editar — só `admin`).
- Edição em `Dialog` local (`editingTier` state + RHF sem zod, ou campos controlados simples): inputs percent (step 0.01, min 0), fixed_fee (step 0.01, min 0), switch is_active → `creditFeeTiersApi.update(id, { percent: Number(...), fixed_fee: Number(...), is_active })` → toast + invalidar `['credit-fee-tiers']`.
- Estados loading (Skeleton), erro (Alert + retry), vazio.

- [ ] **Step 2: Verificação**

Run: `npm test`, `npm run lint`, `npm run build` — sem erros.

---

## Task 12 (Frontend): Área Fechamento Mensal (`CostClosingTab`)

**Files:**
- Create: `src/components/costs/CostClosingTab.tsx`

- [ ] **Step 1: Implementar a tab**

- Card "Fechar mês": `<Input type="month" value={month} onChange={...} />` (estado `month`, default vazio) + botão "Fechar mês" (`admin`), `disabled` sem mês ou `isPending`.
- Mutation: `costClosingApi.close({ month })`; `onSuccess(result)`: guarda em estado local `lastResult`, toast "Fechamento concluído", invalidar `['orders']` e `['dashboard-orders']` (as camadas mensais mudam).
- Nota: "O fechamento é idempotente — rodar novamente recalcula e sobrescreve o mesmo mês."
- Cartão de resultado (`lastResult`): período `period.start — period.end`; totais `orders`, `products`, `allocations` em 3 colunas; tabela de `components` (Nome, Tipo via `typeLabel`, Base de rateio via `allocationBasisLabel`).
- Estados: loading (Loader2 no botão), erro (toast `err?.response?.data?.error`), vazio (dica de uso).

- [ ] **Step 2: Verificação**

Run: `npm test`, `npm run lint`, `npm run build` — sem erros.

---

## Task 13 (Frontend): `ProductAssociationsDialog` — multi-select + badge de subgrupo

**Files:**
- Modify: `src/components/costs/ProductAssociationsDialog.tsx`

- [ ] **Step 1: Multi-select de componentes via `associate-product-batch`**

Substitua o estado `componentId` por `selectedIds: Set<string>` e o select único pela lista com `Checkbox` dos `availableComponents`; input Qtd permanece único. Mutação:

```ts
const addMutation = useMutation({
  mutationFn: () => costComponentsApi.associateProductBatch({
    product_id: productId,
    cost_component_ids: [...selectedIds],
    quantity: parseInt(quantity) || 1,
  }),
  onSuccess: () => {
    toast.success('Componentes associados ao produto');
    setAddOpen(false);
    setSelectedIds(new Set());
    setQuantity('1');
    qc.invalidateQueries({ queryKey: ['cost-associations', productId] });
  },
  onError: (err: ApiError) => toast.error(`Falha ao associar: ${err?.response?.data?.error || err?.message}`),
});
```

Botão "Associar (N)" desabilitado sem seleção.

- [ ] **Step 2: Badge do subgrupo do produto**

- Query `['product-subgroups']` → `productSubgroupsApi.list()` habilitada com `open`; mapa `id → nome` (`useMemo`).
- Ao selecionar um produto na lista, exibir ao lado do nome um `Badge variant="outline"` com o nome do subgrupo (`product.subgroup_id` + mapa) — e dentro do resultado da busca de produtos, mostrar o badge em cada linha quando houver.

- [ ] **Step 3: Verificação**

Run: `npm test`, `npm run lint`, `npm run build` — sem erros.

---

## Task 14 (Frontend): `OrderFinancialSummary` — camadas de custo

**Files:**
- Modify: `src/components/OrderFinancialSummary.tsx`

- [ ] **Step 1: Implementar as camadas**

```tsx
import { useState } from 'react';
import { ChevronDown, ChevronUp } from 'lucide-react';
import { Button } from '@/components/ui/button';
import type { Order } from '@/types';
import { formatCurrency, marginPercent } from '@/lib/formatters';

export default function OrderFinancialSummary({ order }: Props) {
  const [showMonthly, setShowMonthly] = useState(false);

  return (
    <div className="space-y-2 text-sm">
      {/* Total da venda / Desconto (mantidos como hoje) */}
      <div className="flex justify-between">
        <span className="text-muted-foreground">Custo da venda</span>
        <span>{order.total_cost === null || order.total_cost === undefined ? '-' : formatCurrency(order.total_cost)}</span>
      </div>
      <div>
        <button
          type="button"
          className="w-full flex items-center justify-between text-muted-foreground"
          onClick={() => setShowMonthly((v) => !v)}
          disabled={!order.monthly_allocations?.length}
        >
          <span>Custos mensais rateados</span>
          <span className="flex items-center gap-1">
            {order.monthly_cost_total !== undefined ? formatCurrency(order.monthly_cost_total) : '-'}
            {order.monthly_allocations?.length ? (showMonthly ? <ChevronUp className="h-3 w-3" /> : <ChevronDown className="h-3 w-3" />) : null}
          </span>
        </button>
        {showMonthly && order.monthly_allocations?.length ? (
          <div className="mt-2 space-y-1 pl-3 border-l">
            {order.monthly_allocations.map((alloc) => (
              <div key={alloc.id} className="flex justify-between text-xs">
                <span>{alloc.cost_component_name ?? alloc.cost_component_id.slice(0, 8)}</span>
                <span>{formatCurrency(Number(alloc.amount))}</span>
              </div>
            ))}
          </div>
        ) : null}
      </div>
      <div className="flex justify-between font-medium">
        <span>Custo total (venda + mensal)</span>
        <span>{order.total_cost_with_monthly !== undefined ? formatCurrency(order.total_cost_with_monthly) : '-'}</span>
      </div>
      {/* Lucro total / Margem (mantidos como hoje, valores da API) */}
    </div>
  );
}
```

(Substitua a linha antiga "Custo total" → `total_cost` pelas três camadas; mantenha Total da venda, Desconto, Lucro total e Margem intactos.)

- [ ] **Step 2: Verificação**

Run: `npm test`, `npm run lint`, `npm run build` — sem erros.
