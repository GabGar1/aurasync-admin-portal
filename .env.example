# Base da API. Em produção (mesma origem) use caminho relativo para o cookie fluir sem CORS.
VITE_API_URL=/api

# Opcional: URL do WebSocket. Se vazia, o cliente deriva wss://<host> automaticamente
# (mesma origem) em https, e ws://localhost:3333 em dev http.
VITE_WS_URL=